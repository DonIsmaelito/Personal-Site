// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Static output — ships minimal JS, fast like gwern.net / stephango.com.
  output: 'static',
});
